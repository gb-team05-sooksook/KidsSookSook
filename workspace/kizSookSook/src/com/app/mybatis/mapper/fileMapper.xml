<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="file">
	<insert id="insert" parameterType="enquiryFileVO">
		insert into tbl_enquiry_file(enquiry_id, enquiry_filePath, enquiry_system_name, enquiry_org_name)
		values(#{enquiryId}, #{enquiryFilePath}, #{enquiryFileSystemName}, #{enquiryFileOrgName})
	</insert>
	
</mapper>